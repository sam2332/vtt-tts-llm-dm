# AI DM Listener - Python Services
# This directory contains the Python-based ML/AI services

# Requirements for the Python service:
# - Flask (API server)
# - pyannote.audio (speaker diarization)
# - openai-whisper (speech-to-text)
# - chromadb (vector database for RAG)
# - sentence-transformers (embeddings)
# - TTS (text-to-speech, e.g., Coqui TTS or piper-tts)

# Install dependencies:
# pip install -r requirements.txt
